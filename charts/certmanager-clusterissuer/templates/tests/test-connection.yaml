apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "certmanager-clusterissuer.fullname" . }}-test-connection"
  labels:
    app.kubernetes.io/name: {{ include "certmanager-clusterissuer.name" . }}
    helm.sh/chart: {{ include "certmanager-clusterissuer.chart" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}
    app.kubernetes.io/managed-by: {{ .Release.Service }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "certmanager-clusterissuer.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
